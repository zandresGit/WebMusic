@model WebMusic.Models.Album
@{
    ViewData["Title"] = "Editar";
}

<style type="text/css">
    .row{
        justify-content: center;
    }
    label{
        color: red;
    }
    .form-group{
        background-color: rgb(0 0 0 / 55%);
        border-radius: 0.25rem;
        border-bottom-width: 16px;
        padding-bottom: 20px;
        margin-bottom: 0px;
        padding-top: 20px;
    }
    .form-control{
        width: 95%;
    }
</style>

<hr />
<br />
<div class="row">
    <div class="col-md-4">
        <form asp-action="EditAlbum" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="IdBanda" />
            <partial name="_Album" />
            <div class="form-group" align="center">
                <input type="submit" value="Guardar" class="btn btn-primary" />
                
            </div>
        </form>
    </div>
    <div class="col-md-4">
        <img src="@Model.ImageFullPath" style="width:200px;height:200px;max-width: 100%; height: auto;" />
    </div>
</div>
<div align="right">
    <a asp-action="DetailsBanda" asp-route-id="@Model.IdBanda" class="btn btn-success">Volver a la lista</a>
</div>


@section Scripts {
@{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
}
}